class Solution:
    def thirdMax(self, nums: List[int]) -> int:
        nums_unique=list(set(nums))
        if len(nums_unique)<=2: return max(nums_unique)
        nums_unique.sort(reverse=True)
        return nums_unique[2]
        
        

    
